<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

	<link href="stylesheets/materialize.css" type="text/css" media="screen,projection" />
	<link rel="stylesheet" href="/stylesheets/style.css" />
	<!--Let browser know website is optimized for mobile-->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	
	<link rel="shortcut icon" href=".././images/icon.ico" />
	<title> Kinofil - <%= movie.title %> </title>
</head>

<body>
	<!--NAVBAR-->
	<%- include("./navbar.ejs") %>

	<nav class="orange">
		<div class="nav-wrapper orange container">
			<div class="col s12">
				<a href="/" class="breadcrumb">Home</a>
				<a href="/movies" class="breadcrumb">Movies</a>
				<a href="#!" class="breadcrumb"><%=movie.title%></a>
			</div>
		</div>
	</nav>

	<div class="section no-pad-bot" id="index-banner">
		<div class="container">

			<div class="row">
				<div class="col s6 m4">
					<div class="card-image ">
						<img src=<%=movie.poster %> height="500pt">
					</div>
				</div>

				<div class="col s6 m8">
					<div class="card white ">
						<div class="card-content ">
							<span class="card-title"><%= movie.title %></span>
							<span class="card-title"><% 
								let reviews=movie.reviews || [0];
								let sum=reviews.reduce(
								(a,b)=>parseInt(a)+parseInt(b), 0);
								let prom=sum/reviews.length;
								%> <%= prom %></span>
							<p><%= movie.overview %></p>
						</div>
						<div class="card-action">
							<% if (login && user.watched.includes(`${movie._id}`)) { %>
							<a id="rem-watched">Remove from watched</a>
							<% } else { %>
							<a id="add-watched">Add to watched</a>
							<% } %>

							<% if (login && user.watchlist.includes(`${movie._id}`)) { %>
							<a id="rem-watchlist">Remove from watchlist</a>
							<% } else { %>
							<a id="add-watchlist">Add to watchlist</a>
							<% } %>

							<% if (login && user.watching.includes(`${movie._id}`)) { %>
							<a id="rem-watching">Remove from watching</a>
							<% } else { %>
							<a id="add-watching">Add to watching</a>
							<% } %>


							<% if (login && user.favorites.includes(`${movie._id}`)) { %>
							<a id="rem-favorites">Remove from favorites</a>
							<% } else { %>
							<a id="add-favorites">Add to favorites</a>
							<% } %>

						</div>


					</div>
				</div>
			</div>
		</div>
	</div>
	<script type="text/html" src="javascripts/movie.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>